<div class="container">
  <div class="row">
    <h1>Contact</h1>

    <% data.contacts.each do |contact| %>
      <div class="col-xs-6 col-md-3">
        <h3><%= contact.name %></h3>
        <%= image_tag(contact.picture, class: 'img-responsive') %>
      </div>
    <% end %>


  </div>
</div>
